// Extracted from main; webpack module 40833. Factory, not an ES module.
((Ae, V, i) => {
  "use strict";

  function t(e, a) {
    if (a.length < e) throw new TypeError(e + " argument" + (e > 1 ? "s" : "") + " required, but only " + a.length + " present");
  }
  i.d(V, {
    Z: () => t
  });
});
