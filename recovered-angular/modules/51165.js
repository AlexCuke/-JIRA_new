// Extracted from main; webpack module 51165. Factory, not an ES module.
((Ae, V, i) => {
  "use strict";

  function t(e) {
    return e instanceof Date && !isNaN(e);
  }
  i.d(V, {
    q: () => t
  });
});
